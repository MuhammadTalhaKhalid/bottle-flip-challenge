# Training / evaluation / ONNX-export deps.
# Install the torch build for your platform first (CUDA from pytorch.org, or CPU):
#   pip install torch torchvision
torch>=2.6
torchvision>=0.21
numpy>=1.26
scikit-learn>=1.4
opencv-python>=4.11
ultralytics>=8.3

# ONNX export for the in-browser app (src/export_onnx.py)
onnx>=1.16
onnxruntime>=1.18
