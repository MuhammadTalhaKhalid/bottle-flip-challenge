# Web apps (Streamlit live/upload UI + FastAPI service).
# Install torch/torchvision separately for your platform (CUDA or CPU) first:
#   pip install torch torchvision        (or the CUDA build from pytorch.org)
streamlit>=1.40
streamlit-webrtc>=0.47
av>=12.0
fastapi>=0.110
uvicorn[standard]>=0.27
ultralytics>=8.3
opencv-python>=4.11
numpy>=1.26
scikit-learn>=1.4
